/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/937-mission-control",
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
};

export default nextConfig;
